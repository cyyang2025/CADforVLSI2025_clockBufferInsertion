# CADforVLSI2025_clockBufferInsertion
Clock buffer insertion plays a pivotal role in clock tree synthesis (CTS), which distributes a synchronous clock with bounded fanout and wire length budgets while seeking low clock skew and clock tree wire length. In this project, the goal to design a rectilinear-length-based clock tree by inserting levelized clock buffers hierarchically.
